{
  "name": "pi-steering-hooks-monorepo",
  "version": "0.0.0-poc.0",
  "private": true,
  "description": "PoC monorepo for @cad0p/pi-steering-hooks (steering engine) and unbash-walker (AST utility).",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "typescript": "^5.4.5"
  }
}
