/**
 * unbash-walker — AST walking utility for unbash `Script` trees.
 *
 * Scaffolded placeholder. Real implementation arrives in Phase 1 of the
 * pi-steering-hooks PoC. See ../README.md for the full plan.
 */

const NOT_IMPLEMENTED_MESSAGE =
	"unbash-walker is scaffolded only — implementation arrives in Phase 1. " +
	"See https://github.com/cad0p/pi-steering-hooks for status.";

export function __scaffold__(): never {
	throw new Error(NOT_IMPLEMENTED_MESSAGE);
}
