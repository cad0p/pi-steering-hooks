{
	"name": "@examples/dynamic-reason-runtime-cwd",
	"version": "0.0.0-example",
	"private": true,
	"description": "Worked example: dynamic-reason + walker-unknown-cwd pattern (requireKnownCwd + walkerUnknownCwdReason).",
	"type": "module",
	"peerDependencies": {
		"pi-steering": "workspace:*"
	},
	"devDependencies": {
		"pi-steering": "workspace:*"
	},
	"scripts": {
		"pretest": "pnpm --filter pi-steering run clean && pnpm --filter pi-steering run build",
		"test": "node --test --experimental-strip-types 'steering.test.ts'",
		"typecheck": "tsc --noEmit"
	}
}
